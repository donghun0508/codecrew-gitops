apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: infrastructure-helm
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]

  generators:
    # Helm chart 있는 케이스
    - git:
        repoURL: https://github.com/donghun0508/codecrew-gitops
        revision: main
        files:
          - path: "infrastructure/*/envs/*/app.yaml"
      selector:
        matchExpressions:
          - key: chart
            operator: Exists

  template:
    metadata:
      name: "infra-{{.name}}-{{index .path.segments 3}}"
    spec:
      project: default
      destination:
        server: https://kubernetes.default.svc
        namespace: "{{.namespace}}"
      sources:
        - repoURL: "{{.repo}}"
          chart: "{{.chart}}"
          targetRevision: "{{.version}}"
          helm:
            releaseName: "{{.name}}"
            valueFiles:
              - "$values/{{index .path.segments 0}}/{{index .path.segments 1}}/{{index .path.segments 2}}/{{index .path.segments 3}}/values.yaml"
        - repoURL: https://github.com/donghun0508/codecrew-gitops
          targetRevision: main
          ref: values
          path: "."
        - repoURL: https://github.com/donghun0508/codecrew-gitops
          targetRevision: main
          path: "{{index .path.segments 0}}/{{index .path.segments 1}}/{{index .path.segments 2}}/{{index .path.segments 3}}"
          directory:
            recurse: true
            exclude: "values.yaml"
      ignoreDifferences:
        - group: apps
          kind: StatefulSet
          jsonPointers:
            - /spec/volumeClaimTemplates
        - group: apps
          kind: StatefulSet
          jqPathExpressions:
            - .spec.template.metadata.annotations
        - group: ""
          kind: PersistentVolumeClaim
          jqPathExpressions:
            - .spec.volumeName
        - group: ""
          kind: Service
            - .spec.clusterIP
            - .spec.clusterIPs
            - .spec.ports[].nodePort
        # Agones Ignore Rules
        - group: ""
          kind: Secret
          name: agones-cert
          jsonPointers:
            - /data
        - group: ""
          kind: Secret
          name: allocator-tls
          jsonPointers:
            - /data
        - group: ""
          kind: Secret
          name: allocator-client-ca
          jsonPointers:
            - /data
        - group: admissionregistration.k8s.io
          kind: MutatingWebhookConfiguration
          jqPathExpressions:
            - .webhooks[].clientConfig.caBundle
        - group: admissionregistration.k8s.io
          kind: ValidatingWebhookConfiguration
          jqPathExpressions:
            - .webhooks[].clientConfig.caBundle
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true