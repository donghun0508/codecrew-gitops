fullnameOverride: account-service

replicaCount: 1

image:
  repository: ghcr.io/donghun0508/account-service
  tag: 1ec098636dd93dea0fcbcb2abc622d1056d33dac
  pullPolicy: IfNotPresent

imagePullSecrets:
  - name: ghcr-secret

containerPort: 8080

service:
  type: ClusterIP
  port: 80
  targetPort: http

ingress:
  enabled: false

env:
  - name: SPRING_PROFILES_ACTIVE
    value: prod

monitoring:
  enabled: true
  namespace: prometheus
  labels:
    release: prometheus
  path: /actuator/prometheus
  interval: 15s
  portName: http
