fullnameOverride: member-service-prod

replicaCount: 1

image:
  repository: ghcr.io/donghun0508/member-service
  tag: 49e6e9f4e4d2729749e323f370327f182ad8663a
  pullPolicy: IfNotPresent

imagePullSecrets:
  - name: ghcr-secret

containerPort: 8080

service:
  type: ClusterIP
  port: 80
  targetPort: http

ingress:
  enabled: false

env:
  - name: SPRING_PROFILES_ACTIVE
    value: prod

monitoring:
  enabled: true
  namespace: prometheus
  labels:
    release: prometheus
  path: /actuator/prometheus
  interval: 15s
  portName: http
